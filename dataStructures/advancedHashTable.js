class HashTable {
  constructor() {
    this.storage = [];
    this.limit = 15;
  }

  hashKeyBelowLimit(str, max) {
    let hash = 0;

    for (let i = 0; i < str.length; i++) {
      let code = key.
    }
  }
}

// Creating an object that doesnt have any prototypes:
// const map = Object.create(null);
